[project]
name = "ai-assistant-dotfiles"
version = "0.1.0"
description = "Shared Claude, Codex, and OpenCode dotfiles and setup scripts"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
exclude-newer = "7 days"
